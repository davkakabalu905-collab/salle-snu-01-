version: '3.8'

services:
  backend:
    build: .
    container_name: claudia_backend
    ports:
      - "5050:5050"
    env_file:
      - .env
    restart: unless-stopped
    security_opt:
      - no-new-privileges:true
